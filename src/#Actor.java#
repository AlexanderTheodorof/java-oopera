class Actor extends Person {                   // актер
    //String name;
    //String surname;
    //Gender gender;
    private int   height;

    Actor(Person person, int height) {
        super(person.getName, person.getSurname, person.getGender);
    }

    private int getHeight() {
        return height;
    }
}
